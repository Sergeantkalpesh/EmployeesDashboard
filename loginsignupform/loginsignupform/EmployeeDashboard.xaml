<Window x:Class="loginsignupform.EmployeeDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:loginsignupform"
        mc:Ignorable="d"
        Title="EmployeeDashboard" Height="500" Width="800" WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">

    <Grid Margin="20,20,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="8" BorderBrush="#CCCCCC" BorderThickness="1" Background="White">
            <DataGrid x:Name="employeeGrid" AutoGenerateColumns="False" CanUserAddRows="False"
              AlternationCount="2"
              RowBackground="White"
              AlternatingRowBackground="#F9F9F9"
              GridLinesVisibility="Horizontal"
              HorizontalGridLinesBrush="#E0E0E0"
              VerticalGridLinesBrush="#E0E0E0"
              FontSize="14"
              RowHeight="30"
              ColumnHeaderHeight="35"
              HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Employee Name" Binding="{Binding EmpName}" Width="*" Foreground="#FFD61717"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding EmpEmail}" Width="*"/>
                    <DataGridTextColumn Header="Number" Binding="{Binding Number}" Width="80"/>
                    <DataGridTextColumn Header="Designation" Binding="{Binding Designation}" Width="*"/>
                    <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="2*"/>
                    <DataGridTemplateColumn Header="Actions" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Edit" Style="{StaticResource BlueButtonStyle}" Width="70" Margin="5" Click="Edit_Click"/>
                                    <Button Content="Delete"  Style="{StaticResource RedButtonStyle}" Width="70" Margin="5" Click="Delete_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>


        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Add Employee" Style="{StaticResource BlueButtonStyle}" Width="120" Height="30" Click="AddEmployee_Click"/>
        </StackPanel>
    </Grid>
</Window>
